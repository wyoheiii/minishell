#ifndef EXPAND_IS_H
# define EXPAND_IS_H

# include <stdbool.h>
# include <stdlib.h>

# include "libft.h"
# include "expand_struct.h"

bool	is_remove(t_expand *list);
bool	is_expand(t_expand *list);

#endif
